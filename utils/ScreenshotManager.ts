/**
 * @file ScreenshotManager.ts
 * @description Re-exports ScreenshotHelper for backward-compatible imports.
 * Use `ScreenshotHelper.ts` directly in new code.
 */
export { milestone, captureFailure } from './ScreenshotHelper';
