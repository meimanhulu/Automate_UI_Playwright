/**
 * @file ValidationLogger.ts
 * @description Re-exports ExecutionLogger for backward-compatible imports.
 * Use `ExecutionLogger.ts` directly in new code.
 */
export { logger } from './ExecutionLogger';
