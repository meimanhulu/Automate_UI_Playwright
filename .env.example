# ─── App Under Test ───────────────────────────────────────────────────────────
# URL of the main page that has the QRIS order form
APP_URL=http://localhost:3000

# ─── Alto Payment API ─────────────────────────────────────────────────────────
# Base URL of the payment gateway (no trailing slash)
API_BASE_URL=https://mmsapi-test.manjo.co.id

# API key used in X-Alto-Key header and HMAC plaintext
API_KEY=akey_NjwW73gz90xmtDtpbgAj

# Validation key used to sign the HMAC-SHA256 signature
VALIDATION_KEY=vkey_TLLbu9qp3Uj94j4ks2DU

# (Optional) Status check endpoint — only needed if you use waitUntilPaid()
# API_STATUS_ENDPOINT=/altopay/qr-payment/status

# ─── Legacy / Other ───────────────────────────────────────────────────────────
# Base URL alias used by existing tests
BASE_URL=https://example.com

# Credentials used by login.spec.ts / base.fixture.ts
E2E_USERNAME=
E2E_PASSWORD=
